export function normalize(text,lang){let v=String(text||'').toLowerCase().normalize('NFKC');if(lang==='en')return v.replace(/[^a-z0-9' ]/g,'').replace(/\s+/g,' ').trim();if(lang==='ja')return v.replace(/[\s。、！？!?・]/g,'');return v.replace(/[\s.,!?~…“”"']/g,'')}
export function tokenize(text,lang){const n=normalize(text,lang);if(!n)return[];if(lang==='en')return n.split(' ');return [...n]}
export function levenshtein(a,b){const m=a.length,n=b.length,dp=Array.from({length:m+1},()=>Array(n+1).fill(0));for(let i=0;i<=m;i++)dp[i][0]=i;for(let j=0;j<=n;j++)dp[0][j]=j;for(let i=1;i<=m;i++)for(let j=1;j<=n;j++)dp[i][j]=Math.min(dp[i-1][j]+1,dp[i][j-1]+1,dp[i-1][j-1]+(a[i-1]===b[j-1]?0:1));return dp[m][n]}
export function similarity(input,target,lang){const a=tokenize(input,lang),b=tokenize(target,lang);if(!a.length&&!b.length)return 100;return Math.max(0,Math.round((1-levenshtein(a,b)/Math.max(a.length,b.length,1))*100))}
export function diffTokens(input,target,lang){const a=tokenize(input,lang),b=tokenize(target,lang),m=a.length,n=b.length,dp=Array.from({length:m+1},()=>Array(n+1).fill(0));for(let i=1;i<=m;i++)for(let j=1;j<=n;j++)dp[i][j]=a[i-1]===b[j-1]?dp[i-1][j-1]+1:Math.max(dp[i-1][j],dp[i][j-1]);let i=m,j=n,out=[];while(i>0||j>0){if(i>0&&j>0&&a[i-1]===b[j-1]){out.unshift({type:'ok',value:b[j-1]});i--;j--}else if(j>0&&(i===0||dp[i][j-1]>=dp[i-1][j])){out.unshift({type:'miss',value:b[j-1]});j--}else{out.unshift({type:'extra',value:a[i-1]});i--}}return out}
export function dueReviews(state,lessons){return state.mistakes.map((m,index)=>({...m,index,lesson:lessons.find(l=>l.id===m.lessonId)})).filter(x=>x.lesson)}
export function markMistake(state,lessonId,type,input,answer){const old=state.mistakes.find(m=>m.lessonId===lessonId&&m.type===type);if(old){old.count++;old.input=input;old.answer=answer;old.last=Date.now()}else state.mistakes.push({lessonId,type,input,answer,count:1,last:Date.now()})}
export function completeSession(state,name,score){state.daily[name]=true;state.xp+=score>=80?12:score>=55?7:3;state.completed[name]=(state.completed[name]||0)+1}
